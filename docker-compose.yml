version: "3.9"

services:
  frontend:
    build: ./demo_hire
    ports:
      - "2000:2000"
    depends_on:
      - backend
    environment:
      - API_BASE_URL=http://localhost:5000

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=development